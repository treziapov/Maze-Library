package mazelib.data;

/**
 * Interface to facilitate pluggable Algorithm Heuristics.
 * @author Timur Reziapov	<reziapo1@illinois.edu>
 * @date Sunday, September 9, 2012 18:00 PM
 */
public interface Heuristic {
	
	/**
	 * Method that all implementing classes must define.
	 * @param originNode the Node to calculate distance from
	 * @param targetNode the Node to calculate distance to
	 * @return the distance from origin to target according to the Heuristic
	 */
	long calculateDistance(Node originNode, Node targetNode);
	
}
